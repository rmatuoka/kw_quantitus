= form_for [:admin, @event] do |f|
  -if f.error_messages
    .alert.alert-danger.alert-dismissable
      %i.fa.fa-ban
      %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} &times;
      %b Alert!
      Confira os campos marcados em vermelho.
  .box-body
    .form-group
      = f.label :data, "Data do Evento"
      = f.text_field :data, :class=> "form-control"
    .form-group
      = f.label :name, "Nome do Evento"
      = f.text_field :name, :class=> "form-control"
    .form-group
      = f.label :link, "Link de mais informações"
      = f.text_field :link, :class=> "form-control"
    .form-group
      = f.label :order, "Ordem"
      = f.number_field :link, :class=> "form-control"

  .box-footer
    =f.submit "Salvar", :class => "btn btn-primary"
